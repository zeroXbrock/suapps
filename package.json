{
  "name": "suapps",
  "module": "index.ts",
  "devDependencies": {
    "bun-types": "^1.0.16",
    "typescript": "^5.3.3"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "type": "module",
  "dependencies": {
    "commander": "^11.1.0",
    "viem": "link:viem"
  },
  "scripts": {
    "format": "echo 'this is a workaround to bypass suave-viem precommit script hooks'",
    "lint:fix": "echo 'this is a workaround to bypass suave-viem precommit script hooks'",
    "start": "bun run index.ts"
  }
}
